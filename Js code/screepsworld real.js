

// 基礎主程式 （持續按照tick回合制）

module.exports.loop = function () {

	const spawn = Game.spawns['Spawn1']

	}

}

// 清理記憶體

// 計算個角色數量

// 顯示角色數量

// 自動生產

type Game struct {
    Spawns 
}

type Spawn struct {
    Name String
    Energy int
}

func (s *Spawn) 

